# Полный гайд по Claude Code на основе плейлиста Net Ninja

## Обзор курса

**Автор**: Net Ninja (Shaun Pelling)
**Канал**: [Net Ninja - 1.82M подписчиков](https://www.youtube.com/@NetNinja)
**Количество видео**: 10
**Общая длительность**: ~1 час 43 минуты
**Общее количество просмотров**: 362,275
**Последнее обновление**: 4 сентября 2025 года

Этот плейлист представляет собой полное практическое руководство по использованию Claude Code - агентского инструмента для программирования от Anthropic. Курс охватывает все аспекты работы с Claude Code: от базовой установки и настройки до продвинутых техник работы с MCP-серверами, субагентами и интеграцией с GitHub. Claude Code позиционируется как "co-developer" (со-разработчик), который помогает генерировать код, запускать тесты, предоставлять обратную связь и ассистировать в рабочих процессах разработки.

**Ключевые темы плейлиста**:
- Установка и настройка Claude Code
- Работа с контекстом через CLAUDE.md файлы
- Использование slash-команд и инструментов
- MCP-серверы (Model Context Protocol)
- Создание и использование субагентов
- Интеграция с GitHub
- Лучшие практики и советы

---

## 📺 Видео 1: Introduction & Setup

**Ссылка**: [Claude Code Tutorial #1](https://www.youtube.com/watch?v=SUysp3sJHbA)
**Длительность**: 10:11
**Просмотры**: 359K

### Что вы узнаете:
- Установка Claude Code
- Первоначальная настройка
- Основы командной строки
- Создание первого проекта

### 🛠️ Практические примеры команд:

#### Установка Claude Code

```bash
# Установка через npm (требуется Node.js 18+)
npm install -g @anthropic-ai/claude-code

# Или через Homebrew (macOS/Linux)
brew install claude-code

# Проверка версии
claude --version

# Просмотр справки
claude --help
```

#### Инициализация в проекте

```bash
# Переход в директорию проекта
cd /path/to/your/project

# Инициализация Claude Code в проекте (создаст базовый CLAUDE.md)
claude init

# Запуск Claude Code
claude
```

#### Первоначальная настройка

```bash
# Аутентификация
claude auth login

# Проверка статуса аутентификации
claude auth status

# Выход из аккаунта
claude auth logout
```

### 📝 Пример создания базового CLAUDE.md

После инициализации будет создан файл `CLAUDE.md`:

```markdown
# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

[Описание вашего проекта в 2-3 предложениях]

## Build Commands

### Building and Running
```bash
# Build project
./gradlew build

# Run tests
./gradlew test

# Run application
./gradlew run
```

## Architecture

### Tech Stack
- [Ваш язык программирования]
- [Основные фреймворки]
- [База данных]

### Project Structure
```
src/
├── main/
│   ├── java/
│   └── resources/
└── test/
```
```

---

## 📺 Видео 2: CLAUDE.md Files & /init

**Ссылка**: [Claude Code Tutorial #2](https://www.youtube.com/watch?v=i_OHQH4-M2Y)
**Длительность**: 12:18
**Просмотры**: 107K

### Что вы узнаете:
- Структура CLAUDE.md файла
- Использование команды `/init`
- Предоставление контекста проекта
- Лучшие практики документирования

### 🛠️ Команда /init

```bash
# Запуск мастера инициализации
/init

# Это запустит интерактивный мастер с вопросами:
# - What is the project name?
# - What is the project description?
# - What is the tech stack?
# - What are the main directories?
# - What build commands do you use?
```

### 📝 Пример полной структуры CLAUDE.md

#### Базовый шаблон (для небольшого проекта)

```markdown
# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

MyProject is a web application for task management. It uses React for frontend, Node.js for backend, and PostgreSQL for database.

## Build Commands

### Development
```bash
# Install dependencies
npm install

# Start development server
npm run dev

# Run tests
npm test

# Build for production
npm run build
```

### Backend
```bash
# Start backend server
cd backend && npm start

# Run database migrations
npm run migrate
```

## Architecture

### Frontend Structure
```
src/
├── components/    # Reusable React components
├── pages/        # Page components
├── hooks/        # Custom React hooks
├── utils/        # Utility functions
└── App.tsx       # Main app component
```

### Backend Structure
```
server/
├── routes/       # API routes
├── models/       # Database models
├── middleware/   # Express middleware
└── server.js     # Entry point
```

## Tech Stack

- **Frontend:** React 18, TypeScript, TailwindCSS
- **Backend:** Node.js, Express, TypeScript
- **Database:** PostgreSQL with Prisma ORM
- **Testing:** Jest, React Testing Library

## Coding Conventions

- Use functional components with hooks
- Prefer composition over inheritance
- Write TypeScript for all new files
- Follow ESLint configuration

## Common Tasks

### Adding a new component
1. Create component in `src/components/`
2. Export it from index.ts
3. Write tests in `__tests__/`
```

#### Продвинутый шаблон (для enterprise проекта)

```markdown
# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

EnterpriseApp is a multi-tenant SaaS platform for inventory management. Built with microservices architecture using Kotlin Multiplatform for mobile clients and Spring Boot for backend services.

## Build Commands

### Full Build
```bash
# Build all services
./gradlew build

# Build specific service
./gradlew :inventory-service:build

# Run all tests
./gradlew test

# Run integration tests
./gradlew integrationTest
```

### Docker
```bash
# Build all containers
docker-compose build

# Start all services
docker-compose up -d

# View logs
docker-compose logs -f

# Stop all services
docker-compose down
```

### Database
```bash
# Run migrations
./gradlew flywayMigrate

# Rollback last migration
./gradlew flywayUndo

# Clean database
./gradlew flywayClean
```

## Architecture

### Microservices
- **inventory-service** - Product and inventory management
- **order-service** - Order processing
- **user-service** - User authentication and management
- **notification-service** - Email and push notifications

### Module Structure
```
/
├── services/
│   ├── inventory-service/
│   ├── order-service/
│   └── user-service/
├── shared/
│   ├── common/           # Shared utilities
│   └── domain/           # Domain models
├── mobile/
│   ├── android/          # Android app
│   └── ios/              # iOS app
└── infra/
    ├── docker/           # Docker configurations
    └── k8s/              # Kubernetes manifests
```

### Technology Stack

**Backend:**
- Kotlin 1.9, Spring Boot 3.2
- PostgreSQL 15, Redis 7
- Kafka 3.6 for event streaming
- Docker, Kubernetes

**Mobile:**
- Kotlin Multiplatform
- Jetpack Compose (Android)
- SwiftUI (iOS)

**DevOps:**
- GitHub Actions CI/CD
- ArgoCD for GitOps
- Prometheus + Grafana monitoring

## Development Guidelines

### Code Style
- Follow Kotlin coding conventions
- Use detekt for static analysis
- Maximum function complexity: 10
- Maximum function length: 50 lines

### Testing Strategy
- Unit test coverage minimum: 80%
- Integration tests for all API endpoints
- E2E tests for critical user flows

### Security
- Never commit secrets
- Use environment variables for configuration
- All API endpoints require authentication
- Enable CORS only for trusted domains

## Common Workflows

### Adding a new API endpoint
1. Define controller in `*Controller.kt`
2. Implement service layer in `*Service.kt`
3. Add repository in `*Repository.kt`
4. Write unit and integration tests
5. Update API documentation

### Creating a new microservice
1. Copy template from `service-template/`
2. Update package names and dependencies
3. Configure database schema
4. Set up CI/CD pipeline
5. Register in service discovery

## Troubleshooting

### Build fails with "OutOfMemory"
```bash
./gradlew build -Dorg.gradle.jvmargs="-Xmx4096m"
```

### Tests fail locally but pass in CI
```bash
# Clean all caches
./gradlew clean cleanBuildCache

# Rebuild
./gradlew build --refresh-dependencies
```

### Database migration fails
```bash
# Check migration status
./gradlew flywayInfo

# Repair metadata
./gradlew flywayRepair
```

## Important Notes

- Always run `detekt` before committing
- Update API docs when changing endpoints
- Tag releases with semantic versioning
- Monitor service health in Grafana dashboard
```

---

## 📺 Видео 3: Context

**Ссылка**: [Claude Code Tutorial #3](https://www.youtube.com/watch?v=ob-mYGqqFQw)
**Длительность**: 13:03
**Просмотры**: 58K

### Что вы узнаете:
- Как предоставлять контекст файлам
- Ссылки на файлы и директории
- Управление историей диалога
- Лучшие практики работы с контекстом

### 🛠️ Команды для работы с контекстом

#### Добавление файлов в контекст

```bash
# Добавить конкретный файл
# Просто упомяните файл в сообщении:
"Посмотри на файл src/main/java/App.kt и объясни, что он делает"

# Добавить несколько файлов
"Проанализируй файлы src/main/java/App.kt, src/main/java/Utils.kt и src/main/java/Config.kt"

# Добавить всю директорию
"Изучи структуру директории src/components/"

# Использовать wildcard для файлов
"Проверь все .kt файлы в директории src/main/"
```

### 📝 Практические примеры работы с контекстом

#### Пример 1: Анализ кода

```bash
# Простое сообщение Claude Code
"Прочитай файл database/src/commonMain/kotlin/ru/pavlig43/database/NocombroDatabase.kt
и объясни структуру базы данных"
```

#### Пример 2: Рефакторинг с контекстом

```bash
# Предоставление контекста для рефакторинга
"Я хочу рефакторить файл core/src/commonMain/kotlin/ru/pavlig43/core/ComponentCoroutineScope.kt.
Сначала прочитай его, а затем предложи улучшения для читаемости"
```

#### Пример 3: Добавление новой функции

```bash
# Контекст из нескольких файлов
"Посмотри на файлы:
- coreui/src/commonMain/kotlin/ru/pavlig43/coreui/DatePickerDialog.kt
- coreui/src/commonMain/kotlin/ru/pavlig43/coreui/ProjectDialog.kt

Мне нужно создать новый диалог TimePickerDialog по аналогии с DatePickerDialog.
Используй ту же структуру и стиль"
```

#### Пример 4: Понимание архитектуры проекта

```bash
# Запрос к Claude Code
"""
Проанализируй структуру проекта:
1. Прочитай CLAUDE.md
2. Изучи директорию features/form/
3. Посмотри на файл features/form/declaration/src/commonMain/kotlin/ru/pavlig43/declaration/api/DeclarationFormComponent.kt

Объясни, как работает архитектура форм в этом проекте, используя Decompose и Koin
"""
```

#### Пример 5: Отслеживание зависимостей

```bash
# Понимание зависимостей между модулями
"""
Посмотри на следующие файлы:
- build.gradle.kts (root)
- features/form/declaration/build.gradle.kts
- core/build.gradle.kts

Объясни, как модули зависят друг от друга и какие зависимости используются
"""
```

#### Пример 6: Контекст для тестирования

```bash
# Создание тестов с контекстом
"""
Прочитай файл:
database/src/commonMain/kotlin/ru/pavlig43/database/data/declaration/dao/DeclarationDao.kt

Напиши юнит-тесты для всех функций DAO, используя turbines для тестирования Flow
"""
```

---

## 📺 Видео 4: Tools & Permissions

**Ссылка**: [Claude Code Tutorial #4](https://www.youtube.com/watch?v=TU0ZcDFq0e0)
**Длительность**: 4:55
**Просмотры**: 36K

### Что вы узнаете:
- Какие инструменты использует Claude Code
- Настройка прав доступа
- Безопасность при работе с инструментами
- Управление разрешениями на чтение/запись

### 🛠️ Инструменты Claude Code

#### Основные инструменты

| Инструмент | Описание | Пример использования |
|------------|----------|---------------------|
| **Bash** | Выполнение команд в терминале | `./gradlew build` |
| **Read** | Чтение файлов | Чтение исходного кода |
| **Write** | Запись файлов | Создание новых файлов |
| **Edit** | Редактирование файлов | Внесение изменений |
| **Grep** | Поиск в коде | Поиск определений |
| **Glob** | Поиск файлов по шаблону | `**/*.kt` |
| **WebSearch** | Поиск в интернете | Поиск документации |

### ⚙️ Настройка разрешений

#### Файл конфигурации разрешений

Создайте файл `.claude/config.json`:

```json
{
  "permissions": {
    "bash": {
      "allowed": true,
      "restrictedCommands": [
        "rm -rf",
        "dd",
        "mkfs"
      ],
      "requireConfirmation": true
    },
    "fileWrite": {
      "allowed": true,
      "protectedPaths": [
        ".git",
        "node_modules",
        "build"
      ],
      "requireConfirmation": true
    },
    "webSearch": {
      "allowed": true,
      "rateLimit": 10
    }
  }
}
```

### 📝 Практические примеры

#### Пример 1: Чтение и анализ файла

```bash
# Запрос
"Прочитай файл build.gradle.kts и объясни, какие плагины используются"

# Claude Code использует инструмент Read
# Output: Анализ файла с объяснением каждого плагина
```

#### Пример 2: Редактирование с подтверждением

```bash
# Запрос
"Замени все вхождения 'OperationType' на 'MovementType' в database/src/commonMain/kotlin/ru/pavlig43/database/data/transaction/"

# Claude Code:
# 1. Использует Grep для поиска всех файлов
# 2. Показывает список найденных файлов
# 3. Спрашивает подтверждение перед каждым изменением
# 4. Использует Edit для внесения изменений
```

#### Пример 3: Безопасный bash

```bash
# Запрос
"Запусти тесты для модуля database"

# Claude Code:
# 1. Формирует безопасную команду: ./gradlew :database:test
# 2. Показывает команду
# 3. Спрашивает подтверждение
# 4. Выполняет и показывает результат
```

---

## 📺 Видео 5: Planning & Thinking

**Ссылка**: [Claude Code Tutorial #5](https://www.youtube.com/watch?v=MTGJuu9CeMk)
**Длительность**: 11:11
**Просмотры**: 38K

### Что вы узнаете:
- Режим планирования (Planning mode)
- Режим мышления (Thinking mode)
- Когда использовать каждый режим
- Улучшение качества кода через планирование

### 🛠️ Режим планирования (Planning Mode)

#### Активация режима планирования

```bash
# Используйте slash команду
/plan

# Или добавьте флаг в запрос
--plan
```

### 📝 Пример использования режима планирования

```bash
"""
/plan
Мне нужно создать новый модуль для управления пользователями.

Требования:
1. CRUD операции для пользователей
2. Аутентификация через JWT
3. Ролевая модель доступа
4. Валидация данных
5. Unit и integration тесты

Создай план реализации с шагами
"""
```

#### Пример ответа Claude Code в режиме планирования

```
# План реализации модуля управления пользователями

## Шаг 1: Структура модуля (5 минут)
- [ ] Создать директорию features/user/
- [ ] Настроить build.gradle.kts
- [ ] Добавить зависимости в settings.gradle.kts

## Шаг 2: Domain слой (15 минут)
- [ ] Создать data class User
- [ ] Создать enum UserRole
- [ ] Определить repository interface

## Шаг 3: Data слой (20 минут)
- [ ] Реализовать UserDao с Room
- [ ] Создать UserRepositoryImpl
- [ ] Добавить миграции базы данных

## Шаг 4: Business логика (20 минут)
- [ ] Создать UserService с бизнес-правилами
- [ ] Реализовать валидацию данных
- [ ] Добавить обработку ошибок

## Шаг 5: Presentation слой (25 минут)
- [ ] Создать UserFormComponent
- [ ] Реализовать UI с Jetpack Compose
- [ ] Добавить навигацию через Decompose

## Шаг 6: DI конфигурация (10 минут)
- [ ] Создать UserModule для Koin
- [ ] Зарегистрировать модуль в root component

## Шаг 7: Тестирование (30 минут)
- [ ] Unit тесты для UserService
- [ ] Integration тесты для UserRepository
- [ ] UI тесты для UserFormComponent

## Шаг 8: Документация (10 минут)
- [ ] Обновить CLAUDE.md
- [ ] Добавить README в модуль
- [ ] Создать примеры использования

**Общее время:** ~2.5 часа
**Приоритет:** Критические компоненты → Дополнительные функции
```

### 🤔 Режим мышления (Thinking Mode)

#### Активация режима мышления

```bash
# Добавьте флаг --think в запрос
--think
```

### 📝 Пример сложной задачи с режимом мышления

```bash
"""
--think
Проанализируй производительность базы данных в приложении Nocombro.

Проблема: При открытии формы декларации происходит заметная задержка (2-3 секунды).

Задачи:
1. Изучи код DeclarationDao
2. Проверь запросы к базе данных
3. Найди N+1 проблемы
4. Предложи оптимизации

Используй файлы:
- database/src/commonMain/kotlin/ru/pavlig43/database/data/declaration/dao/DeclarationDao.kt
- features/form/declaration/src/commonMain/kotlin/ru/pavlig43/declaration/internal/component/CreateDeclarationComponent.kt
"""
```

#### Пример ответа с режимом мышления

```
# Анализ производительности DeclarationDao

## 🤔 Анализ проблемы

### Текущая реализация
Изучив `DeclarationDao.kt`,我发现:

1. **Основной запрос** - `getDeclarationById(id: Long)`:
   - Выполняет SELECT для декларации
   - Загружает связанные сущности отдельными запросами

2. **Найденные проблемы**:
   ```
   Для каждой декларации выполняется:
   - 1 запрос для Declaration
   - N запросов для Products (N+1 problem)
   - M запросов для Documents
   ```

### Корневая причина
Room выполняет ленивую загрузку related entities, что приводит к множественным запросам.

## 💡 Предлагаемое решение

### Оптимизация 1: Использование @Relation
```kotlin
@Transaction
@Query("SELECT * FROM declaration WHERE id = :id")
suspend fun getDeclarationWithProducts(id: Long): DeclarationWithProducts
```

### Оптимизация 2: Индексы
```kotlin
@Entity(
    tableName = "declaration",
    indices = [
        Index(value = ["vendorId"]),
        Index(value = ["date"])
    ]
)
```

### Оптимизация 3: Кэширование
```kotlin
class CachedDeclarationRepository(
    private val dao: DeclarationDao,
    private val cache: LruCache<Long, Declaration>
) {
    suspend fun getById(id: Long): Declaration {
        return cache.get(id) ?: dao.getById(id).also { cache.put(id, it) }
    }
}
```

## 📊 Ожидаемые результаты
- Текущее время: 2-3 секунды
- После оптимизации 1: 800-1000ms (-70%)
- После оптимизации 2: 500-700ms (-85%)
- После оптимизации 3: 200-300ms (-90%)

## 🎯 План внедрения
1. Создать data class DeclarationWithProducts
2. Обновить DAO с @Transaction
3. Добавить индексы в Entity
4. Реализовать кэш в Repository
5. Протестировать изменения
6. Задеплоить в staging

Хочешь, чтобы я реализовал эти оптимизации?
```

### 📊 Сравнение режимов

| Характеристика | Planning Mode | Thinking Mode |
|----------------|--------------|---------------|
| **Назначение** | Создание плана действий | Глубокий анализ |
| **Вывод** | Структурированные шаги | Рассуждения + решение |
| **Когда использовать** | Новые функции, рефакторинг | Сложные задачи, отладка |
| **Длительность** | Быстрый | Более медленный |
| **Детализация** | Высокий уровень | Низкоуровневый |

---

## 📺 Видео 6: Slash Commands

**Ссылка**: [Claude Code Tutorial #6](https://www.youtube.com/watch?v=52KBhQqqHuc)
**Длительность**: 12:10
**Просмотры**: 44K

### Что вы узнаете:
- Что такое slash команды
- Создание кастомных slash команд
- Параметризованные команды
- Лучшие практики

### 📚 Что такое Slash Commands?

Slash команды - это переиспользуемые промпты, хранящиеся в файлах markdown в директории `.claude/commands/`.

### 📁 Структура директории

```
project/
├── .claude/
│   └── commands/
│       ├── review.md
│       ├── test.md
│       ├── refactor.md
│       └── docs.md
└── CLAUDE.md
```

### 📝 Примеры slash команд

#### Команда 1: Code Review (`review.md`)

Создайте файл `.claude/commands/review.md`:

```markdown
---
description: Провести код-ревью текущих изменений
---

Проведи полный код-ревью следующих изменений:

## Что проверить:
1. **Правила кодинга**: Следуют ли изменения стандартам проекта?
2. **Архитектура**: Соответствует ли изменение архитектуре проекта?
3. **Производительность**: Есть ли потенциальные проблемы?
4. **Безопасность**: Есть ли уязвимости?
5. **Тесты**: Покрыты ли изменения тестами?

## Формат отчета:
### 🎯 Общая оценка
- Качество кода: [1-10]
- Сложность: [Низкая/Средняя/Высокая]
- Готовность к мерджу: [Да/Нет]

### 📋 Найденные проблемы
- [Критические]
- [Средние]
- [Минорные]

### 💡 Рекомендации
- [Улучшения]

### ✅ Что сделано хорошо
- [Позитивные моменты]

Используй контекст из git diff и изучи все измененные файлы.
```

**Использование:**
```bash
/review
```

#### Команда 2: Генерация тестов (`test.md`)

Создайте файл `.claude/commands/test.md`:

```markdown
---
description: Создать unit тесты для указанного файла
---

Напиши полные unit тесты для файла: $FILE

## Требования:
1. Используй JUnit 5 и Mockito
2. Покрой все публичные методы
3. Добавь тесты для граничных случаев
4. Добавь тесты для обработки ошибок
5. Следуй принципу AAA (Arrange, Act, Assert)

## Формат:
```kotlin
class ClassNameTest {

    @Test
    fun `should do something when condition`() {
        // Arrange
        // Act
        // Assert
    }
}
```

Сначала прочитай файл, затем создай тестовый файл в том же пакете с суффиксом 'Test'.
```

**Использование:**
```bash
/test $FILE
```

**Пример:**
```bash
/test UserService.kt
```

#### Команда 3: Рефакторинг (`refactor.md`)

Создайте файл `.claude/commands/refactor.md`:

```markdown
---
description: Рефакторить код для улучшения читаемости
---

Проанализируй и предложи рефакторинг для: $FILE

## Цели рефакторинга:
1. Улучшить читаемость
2. Уменьшить сложность
3. Следовать принципам SOLID
4. Улучшить тестируемость

## Что проверить:
- Длина функций (>50 строк)
- Вложенность (>3 уровней)
- Дублирование кода
- Магические числа и строки
- Имена переменных и функций
- Комментарии (если они нужны, код неочевиден)

## Вывод:
1. Покажи текущий код
2. Укажи проблемы
3. Предложи улучшенную версию
4. Объясни изменения
```

**Использование:**
```bash
/refactor ComponentCoroutineScope.kt
```

#### Команда 4: Документация (`docs.md`)

Создайте файл `.claude/commands/docs.md`:

```markdown
---
description: Сгенерировать документацию для модуля
---

Создай документацию для модуля: $MODULE

## Содержание:
1. **Overview**: Назначение модуля
2. **Architecture**: Как организован код
3. **API**: Публичные интерфейсы
4. **Examples**: Примеры использования
5. **Dependencies**: Зависимости от других модулей

## Формат:
```markdown
# Module Name

## Overview
[Описание]

## Architecture
[Архитектура]

## API
### Public Classes
- \`ClassName\`: [Описание]

### Public Functions
- \`functionName()\`: [Описание]

## Usage Example
\`\`\`kotlin
[Пример кода]
\`\`\`

## Dependencies
- \`module1\`: [Зачем нужен]
- \`module2\`: [Зачем нужен]
\`\`\`

Сохрани документацию в файл $MODULE/README.md
```

**Использование:**
```bash
/docs core
```

### 🔧 Параметризованные команды

#### Команда с несколькими параметрами (`create-feature.md`)

```markdown
---
description: Создать новый feature модуль
---

Создай новый feature модуль с именем: $NAME

## Параметры:
- Имя модуля: $NAME
- Тип: $TYPE [form|table|screen]
- С базой данных: $HAS_DB [true|false]

## Шаги создания:
1. Создать структуру директорий
2. Создать build.gradle.kts
3. Добавить в settings.gradle.kts
4. Создать базовые классы
5. Добавить DI модуль

## Структура:
\`\`\`
features/$NAME/
├── src/
│   └── commonMain/kotlin/
│       ├── api/           # Публичный API
│       ├── internal/      # Внутренняя реализация
│       └── di/            # Dependency Injection
└── build.gradle.kts
\`\`\`

После создания покажи список созданных файлов.
```

**Использование:**
```bash
/create-feature --NAME="notifications" --TYPE="screen" --HAS_DB="true"
```

### 🚀 Продвинутые примеры

#### Команда для работы с Git (`git-status.md`)

```markdown
---
description: Показать статус git и предложить действия
---

Покажи текущий статус git и предложи следующие действия.

## Шаги:
1. Выполни `git status`
2. Проанализируй изменения
3. Предложи действия:
   - Что нужно закоммитить
   - Что нужно добавить в .gitignore
   - Какие branch-и доступны
   - Нужен ли мердж конфликтов

## Формат вывода:
### 📊 Текущий статус
[git status output]

### 🎯 Рекомендуемые действия
- [ ] [Действие 1]
- [ ] [Действие 2]

### ⚠️ Проблемы
- [Если есть]
```

**Использование:**
```bash
/git-status
```

#### Команда для оптимизации производительности (`perf.md`)

```markdown
---
description: Проанализировать производительность кода
---

Проанализируй производительность: $FILE

## Метрики:
1. Сложность функций (цикломатическая сложность)
2. Глубина рекурсии
3. Количество аллокаций
4. Использование кэша
5. Потенциальные блокировки

## Инструменты:
- Используй grep для поиска подозрительных паттернов
- Ищи циклы внутри циклов
- Проверь использование коллекций

## Рекомендации:
Предложи конкретные оптимизации с измеримым эффектом.
```

**Использование:**
```bash
/perf DatabaseManager.kt
```

### 💡 Лучшие практики

1. **Имена команд**: Используй короткие, понятные имена
   - ✅ Хорошо: `/review`, `/test`, `/refactor`
   - ❌ Плохо: `/doCodeReviewAndTesting`, `/generateTestsForFile`

2. **Описание**: Добавь описание в metadata для подсказок
   ```markdown
   ---
   description: Короткое описание команды
   ---
   ```

3. **Параметры**: Используй `$PARAMETER` для параметризации
   ```markdown
   Создай тест для файла: $FILE
   ```

4. **Документация**: Включи примеры использования в теле команды

5. **Модульность**: Разделяй сложные команды на несколько простых

---

## 📺 Видео 7: MCP Servers

**Ссылка**: [Claude Code Tutorial #7](https://www.youtube.com/watch?v=7pKN_pjPW04)
**Длительность**: 13:51
**Просмотры**: 55K

### Что вы узнаете:
- Что такое MCP (Model Context Protocol)
- Установка и настройка MCP servers
- Популярные MCP servers
- Интеграция с внешними сервисами

### 📚 Что такое MCP Servers?

MCP (Model Context Protocol) - это протокол для подключения внешних инструментов и сервисов к Claude Code. MCP servers расширяют возможности Claude Code, позволяя взаимодействовать с:
- Базами данных
- API сервисами
- Файловыми системами
- Различными приложениями

### ⚙️ Установка MCP Servers

#### Файл конфигурации MCP

Создайте файл `.claude/mcp_config.json`:

```json
{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/path/to/allowed/files"],
      "disabled": false
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_TOKEN": "your_github_token_here"
      }
    },
    "postgres": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-postgres", "postgresql://localhost/mydb"],
      "disabled": true
    }
  }
}
```

### 🔧 Популярные MCP Servers

#### 1. Filesystem Server

**Установка:**
```bash
npx -y @modelcontextprotocol/server-filesystem /path/to/project
```

**Конфигурация:**
```json
{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "C:\\Users\\user\\AndroidStudioProjects\\Nocombro"]
    }
  }
}
```

**Использование:**
```bash
# Claude Code может читать и писать файлы через MCP
"Посмотри на все файлы в директории features/form/ через MCP filesystem"
```

#### 2. GitHub Server

**Установка:**
```bash
npm install -g @modelcontextprotocol/server-github
```

**Конфигурация:**
```json
{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_TOKEN": "ghp_xxxxxxxxxxxxxxxxxxxx"
      }
    }
  }
}
```

**Использование:**
```bash
# Создание issue
"Создай GitHub issue в репозитории myorg/myrepo с заголовком 'Bug: Login fails' и описанием ошибки"

# Чтение issues
"Покажи все открытые issues в репозитории"

# Создание PR
"Создай pull request из branch feature/new-ui в main с описанием изменений"
```

#### 3. PostgreSQL Server

**Установка:**
```bash
npm install -g @modelcontextprotocol/server-postgres
```

**Конфигурация:**
```json
{
  "mcpServers": {
    "postgres": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-postgres", "postgresql://user:password@localhost:5432/mydb"]
    }
  }
}
```

**Использование:**
```bash
# Выполнение SQL запросов
"Выполни SELECT * FROM users WHERE email = 'test@example.com' через MCP postgres"

# Анализ схемы базы данных
"Покажи схему базы данных и все таблицы"
```

#### 4. Playwright Server (для веб-тестирования)

**Установка:**
```bash
npm install -g @microsoft/playwright-mcp-server
```

**Конфигурация:**
```json
{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@microsoft/playwright-mcp-server"]
    }
  }
}
```

**Использование:**
```bash
# Тестирование веб-страницы
"Открой https://example.com через Playwright и сделай скриншот"

# Автоматизация браузера
"Перейди на сайт, заполни форму логина и нажми кнопку Submit"
```

### 📝 Практические примеры

#### Пример 1: Интеграция с GitHub

```bash
"""
Используй MCP GitHub server для:
1. Создания issue для бага
2. Назначения issue на разработчика
3. Связывания issue с проектом

Детали бага:
- Заголовок: Declaration form crashes on null vendor
- Описание: When creating a new declaration without selecting a vendor, the app crashes
- Приоритет: High
- Метки: bug, crash, high-priority
"""
```

#### Пример 2: Работа с базой данных через MCP

```bash
"""
Используй MCP PostgreSQL server для:
1. Проверки схемы таблицы declaration
2. Выборки всех деклараций за последний месяц
3. Анализа производительности запросов

База данных: NocombroDatabase
Таблица: declaration
"""
```

#### Пример 3: Автоматизация через Playwright

```bash
"""
Используй MCP Playwright server для:
1. Открытия приложения http://localhost:8080
2. Логина с тестовым пользователем
3. Создания новой декларации
4. Проверки, что декларация сохранена
5. Создания скриншота результата
"""
```

### 🔌 Дополнительные MCP Servers

#### Brave Search Server
```json
{
  "mcpServers": {
    "brave-search": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-brave-search"],
      "env": {
        "BRAVE_API_KEY": "your_api_key"
      }
    }
  }
}
```

#### Google Drive Server
```json
{
  "mcpServers": {
    "google-drive": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-gdrive"],
      "env": {
        "GDRIVE_API_KEY": "your_api_key"
      }
    }
  }
}
```

#### Slack Server
```json
{
  "mcpServers": {
    "slack": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-slack"],
      "env": {
        "SLACK_TOKEN": "xoxb-your-token",
        "SLACK_CHANNEL_ID": "C1234567890"
      }
    }
  }
}
```

### 🛠️ Создание собственного MCP Server

#### Простой MCP Server на Python

```python
# mcp_server.py
from mcp.server import Server
from mcp.server.stdio import stdio_server

app = Server("my-custom-server")

@app.tool("calculate_metrics")
async def calculate_metrics(file_path: str) -> str:
    """Calculate code metrics for a file"""
    # Ваша логика
    lines = count_lines(file_path)
    complexity = calculate_complexity(file_path)
    return f"Lines: {lines}, Complexity: {complexity}"

async def main():
    async with stdio_server() as (read_stream, write_stream):
        await app.run(
            read_stream,
            write_stream,
            app.create_initialization_options()
        )

if __name__ == "__main__":
    import asyncio
    asyncio.run(main())
```

**Конфигурация:**
```json
{
  "mcpServers": {
    "custom": {
      "command": "python",
      "args": ["mcp_server.py"]
    }
  }
}
```

---

## 📺 Видео 8: Subagents

**Ссылка**: [Claude Code Tutorial #8](https://www.youtube.com/watch?v=Phr7vBx9yFQ)
**Длительность**: 10:55
**Просмотры**: 46K

### Что вы узнаете:
- Что такое subagents
- Создание специализированных subagents
- Когда использовать subagents вместо slash команд
- Примеры практического применения

### 📚 Что такое Subagents?

Subagents - это специализированные агенты с собственными инструкциями, которые могут выполнять специфические задачи. В отличие от slash команд, subagents могут:

- Хранить состояние между вызовами
- Иметь собственные инструменты и разрешения
- Быть более автономными
- Выполнять многошаговые задачи

### 📁 Структура директории

```
.claude/
├── agents/
│   ├── tester/
│   │   └── agent.md
│   ├── reviewer/
│   │   └── agent.md
│   └── documenter/
│       └── agent.md
└── commands/
    └── ...
```

### 📝 Примеры Subagents

#### Subagent 1: Tester Agent

Создайте файл `.claude/agents/tester/agent.md`:

```markdown
# Code Testing Agent

## Role
Ты - специализированный агент для написания и запуска тестов. Твоя цель - обеспечить высокое покрытие кода тестами.

## Capabilities
- Чтение и анализ исходного кода
- Написание unit тестов
- Написание integration тестов
- Запуск тестов и анализ результатов
- Создание mock объектов

## Tools
- read: Чтение файлов исходного кода
- write: Создание тестовых файлов
- bash: Запуск тестов (./gradlew test, npm test)
- edit: Редактирование тестовых файлов

## Instructions

### При получении задачи "написать тесты для X":
1. Прочитай файл X
2. Проанализируй публичные методы и классы
3. Определи, что нужно протестировать:
   - Нормальный путь выполнения
   - Граничные случаи
   - Обработка ошибок
4. Создай тестовый файл в правильной директории
5. Напиши тесты следуя принципу AAA (Arrange, Act, Assert)
6. Запусти тесты
7. Если тесты падают, исправь их
8. Покажи отчет о покрытии

### Стандарты тестирования
- Используй JUnit 5 для Kotlin/Java
- Используй Jest для TypeScript/JavaScript
- Название тестов должно быть описательным: \`should do X when Y\`
- Минимальное покрытие: 80%
- Используй mock только когда необходимо

### Пример хорошего теста
\`\`\`kotlin
@Test
fun \`should return user when valid id provided\`() {
    // Arrange
    val userId = 1L
    val expectedUser = User(id = userId, name = "Test")
    every { userRepository.findById(userId) } returns expectedUser

    // Act
    val result = userService.getUserById(userId)

    // Assert
    assertEquals(expectedUser, result)
    verify { userRepository.findById(userId) }
}
\`\`\`

## Output Format
При завершении задачи предоставь:
1. Список созданных тестовых файлов
2. Статистику покрытия
3. Пройденные/непрошедшие тесты
4. Рекомендации по улучшению
```

**Использование:**
```bash
# Используй slash команду для вызова subagent
/test-agent UserService.kt

# Или обратись напрямую
"Попроси tester agent написать тесты для DeclarationDao"
```

#### Subagent 2: Code Reviewer Agent

Создайте файл `.claude/agents/reviewer/agent.md`:

```markdown
# Code Reviewer Agent

## Role
Ты - эксперт по качеству кода. Твоя задача - проводить глубокий анализ кода и находить потенциальные проблемы.

## Capabilities
- Анализ кода на соответствие стандартам
- Поиск багов и уязвимостей
- Проверка архитектурных принципов
- Анализ производительности
- Рекомендации по рефакторингу

## Tools
- read: Чтение файлов
- grep: Поиск паттернов в коде
- bash: Запуск линтеров и анализаторов
- web: Поиск лучших практик

## Review Checklist

### 1. Качество кода
- [ ] Следует ли код соглашениям проекта?
- [ ] Понятны ли имена переменных и функций?
- [ ] Нет ли дублирования?
- [ ] Правильно ли обработаны ошибки?

### 2. Архитектура
- [ ] Соответствует ли изменение архитектуре проекта?
- [ ] Нет ли circular dependencies?
- [ ] Правильно ли использованы паттерны проектирования?
- [ ] Разделены ли слои (domain/data/presentation)?

### 3. Производительность
- [ ] Нет ли утечек памяти?
- [ ] Оптимизированы ли запросы к БД?
- [ ] Нет ли N+1 проблем?
- [ ] Используется ли кэширование?

### 4. Безопасность
- [ ] Экранированы ли пользовательские вводы?
- [ ] Защищены ли чувствительные данные?
- [ ] Правильна ли аутентификация/авторизация?
- [ ] Нет ли SQL injection?

### 5. Тесты
- [ ] Покрыты ли изменения тестами?
- [ ] Тесты проверяют правильные сценарии?
- [ ] Нет ли flaky тестов?

## Rating System
- **9-10**: Отличный код, готов к мерджу
- **7-8**: Хороший код, минорные правки
- **5-6**: Приемлемо, нужны улучшения
- **3-4**: Проблемный код, нужна доработка
- **1-2**: Плохой код, требует полного переписывания

## Output Format
\`\`\`
# Code Review Report

## 📊 Общая оценка: X/10

### ✅ Сильные стороны
- [Что сделано хорошо]

### ⚠️ Найденные проблемы

#### Критические (должны быть исправлены)
1. **[Проблема]**
   - Файл: \`path/to/file\`
   - Строки: X-Y
   - Описание: [Детали]
   - Рекомендация: [Как исправить]

#### Средние (рекомендуется исправить)
[Список]

#### Минорные (может быть исправлено позже)
[Список]

### 💡 Рекомендации по улучшению
- [Рекомендации]

### 📈 Метрики
- Сложность: X
- Дублирование: X%
- Покрытие тестами: X%
\`\`\`
```

**Использование:**
```bash
/review-agent database/src/commonMain/kotlin/ru/pavlig43/database/data/declaration/dao/DeclarationDao.kt
```

#### Subagent 3: Documentation Agent

Создайте файл `.claude/agents/documenter/agent.md`:

```markdown
# Documentation Agent

## Role
Ты - технический писатель. Твоя задача - создавать четкую, полную и поддерживаемой документацией.

## Capabilities
- Анализ кода и архитектуры
- Создание README файлов
- Генерация API документации
- Написание руководств пользователя
- Создание примеров использования

## Tools
- read: Чтение исходного кода
- write: Создание файлов документации
- glob: Поиск файлов по шаблону

## Documentation Types

### 1. Module README
Создай README для модуля, включающий:
- Overview (что делает модуль)
- Features (основные функции)
- Installation (как установить)
- Usage (примеры использования)
- API (публичный интерфейс)
- Dependencies (зависимости)

### 2. API Documentation
Документируй все публичные API:
- Описание класса/функции
- Параметры с типами
- Возвращаемые значения
- Исключения
- Примеры

### 3. Architecture Documentation
Опиши архитектуру:
- Компоненты и их ответственность
- Взаимодействие между компонентами
- Потоки данных
- Диаграммы (в ASCII art)

## Style Guide
- Используй Markdown формат
- Пиши ясно и concisely
- Включай примеры кода
- Используй списки для структурирования
- Добавляй ссылки на связанные документы

## Example Output
\`\`\`markdown
# Declaration Form Module

## Overview
This module provides functionality for creating and managing customs declarations.

## Features
- ✅ Create new declarations
- ✅ Edit existing declarations
- ✅ Validate declaration data
- ✅ Attach documents
- ✅ Generate reports

## Installation
The module is included in the main application. No additional installation required.

## Usage

\`\`\`kotlin
val component = DeclarationFormComponent(
    componentContext = componentContext,
    dependencies = declarationFormDependencies
)

val screen = DeclarationFormScreen(component)
\`\`\`

## API

### DeclarationFormComponent
Main component for declaration form.

#### Properties
- \`stack\`: Navigation stack for form screens
- \`state\`: Current form state

#### Methods
- \`onSave()\`: Saves the declaration
- \`onCancel()\`: Cancels editing

## Architecture
\`\`\`
┌─────────────────────┐
│ DeclarationFormComp │
├─────────────────────┤
│ ┌─────────────────┐ │
│ │  TabsComponent  │ │
│ └─────────────────┘ │
│ ┌─────────────────┐ │
│ │ FilesComponent  │ │
│ └─────────────────┘ │
└─────────────────────┘
\`\`\`
\`\`\`
```

**Использование:**
```bash
/document-agent features/form/declaration
```

### 📊 Slash Commands vs Subagents

| Характеристика | Slash Commands | Subagents |
|----------------|----------------|-----------|
| **Состояние** | Без состояния | Могут сохранять состояние |
| **Сложность** | Простые задачи | Сложные многошаговые задачи |
| **Автономность** | Требуют ручного запуска | Могут работать автономно |
| **Инструкции** | Короткие промпты | Подробные инструкции |
| **Инструменты** | Наследуются | Могут иметь свои |

### 🎯 Когда использовать что?

#### Используйте Slash Commands для:
- ✅ Одноразовых задач
- ✅ Простых трансформаций
- ✅ Быстрых запросов
- ✅ Шаблонных операций

**Примеры:**
- `/review` - быстрый ревью кода
- `/test` - генерация тестов
- `/refactor` - рефакторинг файла

#### Используйте Subagents для:
- ✅ Сложных многошаговых задач
- ✅ Задач с сохранением состояния
- ✅ Специализированной экспертизы
- ✅ Автономной работы

**Примеры:**
- `/tester-agent` - полное покрытие тестами
- `/reviewer-agent` - глубокий анализ кода
- `/documenter-agent` - создание документации

---

## 📺 Видео 9: Claude Code with GitHub

**Ссылка**: [Claude Code Tutorial #9](https://www.youtube.com/watch?v=7pKN_pjPW04)
**Длительность**: 5:47
**Просмотры**: 36K

### Что вы узнаете:
- Интеграция Claude Code с GitHub
- Автоматизация Pull Request reviews
- Работа с Issues
- CI/CD интеграция

### ⚙️ Настройка интеграции с GitHub

#### 1. Установка GitHub MCP Server

```bash
npm install -g @modelcontextprotocol/server-github
```

#### 2. Конфигурация

```json
{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_TOKEN": "ghp_xxxxxxxxxxxxxxxxxxxx"
      }
    }
  }
}
```

#### 3. Получение GitHub Token

```bash
# Создайте Personal Access Token на GitHub
# Settings → Developer settings → Personal access tokens → Tokens (classic)

# Scopes needed:
# - repo (для доступа к репозиториям)
# - pull_requests (для работы с PR)
# - issues (для работы с issues)
```

### 📝 Практические примеры

#### Пример 1: Создание Pull Request

```bash
"""
Используя GitHub MCP server:
1. Создай новую branch feature/add-user-authentication
2. Внеси изменения в код для добавления аутентификации
3. Запушь изменения в удаленный репозиторий
4. Создай Pull Request в main

Название PR: "Add user authentication"
Описание:
- Implemented JWT-based authentication
- Added login/logout endpoints
- Added user registration
- Included unit tests

Используй информацию из git diff для описания изменений
"""
```

#### Пример 2: Code Review для PR

```bash
"""
Проанализируй последний Pull Request в репозитории.

Проверь:
1. Соответствие кода стандартам (detekt)
2. Наличие тестов
3. Производительность
4. Безопасность

Используй GitHub MCP для:
- Чтения файлов из PR
- Получения diff изменений
- Оставления комментария к PR с результатами ревью

Создай комментарий в формате:
## 🤖 Code Review от Claude Code

### ✅ Что хорошо
- [Список]

### ⚠️ Рекомендации
- [Список]

### ❌ Критические проблемы
- [Список]
"""
```

#### Пример 3: Работа с Issues

```bash
"""
Используя GitHub MCP:
1. Получи список всех открытых issues с меткой 'bug'
2. Для каждого issue:
   - Прочитай описание
   - Найди связанный код в репозитории
   - Предложи решение
   - Оставь комментарий с предложением

3. Создай summary report с:
   - Количество обработанных issues
   - Найденные паттерны проблем
   - Рекомендации по улучшению кода
"""
```

#### Пример 4: Автоматизация CI/CD

```bash
"""
Проанализируй и улучши CI/CD пайплайн.

Задачи:
1. Прочитай файл .github/workflows/ci.yml
2. Проверь:
   - Оптимизированы ли шаги?
   - Кэшируются ли зависимости?
   - Параллелизуются ли тесты?
3. Предложи улучшения
4. Создай commit с изменениями
5. Открой PR с описанием оптимизаций

Ожидаемые улучшения:
- Время выполнения: -50%
- Использование кэша: +80%
- Параллельность: 4 потока тестов
"""
```

### 🔧 Slash команды для GitHub

#### Команда: `/pr-create`

Создайте файл `.claude/commands/pr-create.md`:

```markdown
---
description: Создать Pull Request
---

Создай Pull Request используя GitHub MCP.

## Параметры:
- Название: $TITLE
- Описание: $DESCRIPTION
- Бранч: $BRANCH
- База: $BASE (default: main)

## Шаги:
1. Получи текущий git diff
2. Создай осмысленное описание изменений
3. Создай branch если не существует
4. Закоммить изменения
5. Запушь в удаленный репозиторий
6. Создай PR через GitHub API

## Формат описания PR:
\`\`\`markdown
## $TITLE

### Changes
- [Change 1]
- [Change 2]

### Testing
- [Тест 1]
- [Тест 2]

### Checklist
- [x] Tests added
- [x] Documentation updated
- [x] No breaking changes
\`\`\`
```

**Использование:**
```bash
/pr-create --TITLE="Fix database connection leak" --BRANCH="fix/db-leak"
```

#### Команда: `/issue-analyze`

Создайте файл `.claude/commands/issue-analyze.md`:

```markdown
---
description: Проанализировать GitHub issue и предложить решение
---

Используя GitHub MCP, проанализируй issue #$NUMBER.

## Что сделать:
1. Получи детали issue
2. Прочитай прикрепленные файлы кода
3. Проанализируй проблему
4. Найди корневую причину
5. Предложи решение с кодом
6. Оставь комментарий в issue

## Формат комментария:
\`\`\`markdown
## 🔍 Анализ проблемы

### Описание
[Описание проблемы]

### Корневая причина
[Причина]

### 💡 Предложенное решение
\`\`\`kotlin
[Код решения]
\`\`\`

### Шаги внедрения
1. [Шаг 1]
2. [Шаг 2]
3. [Шаг 3]

### Тестирование
- [Как протестировать]
\`\`\`
```

**Использование:**
```bash
/issue-analyze --NUMBER=42
```

---

## 📺 Видео 10: Final Thoughts & Tips

**Ссылка**: [Claude Code Tutorial #10](https://www.youtube.com/watch?v=cCHPjvswTpQ)
**Длительность**: 6:16
**Просмотры**: 18K

### Что вы узнаете:
- Лучшие практики использования Claude Code
- Распространенные ошибки
- Советы по продуктивности
- Будущее Claude Code

### 💡 Лучшие практики

#### 1. Начинайте с хорошего CLAUDE.md

```markdown
# Минимальный шаблон CLAUDE.md

# CLAUDE.md

## Project Overview
[2-3 предложения о проекте]

## Build Commands
\`\`\`bash
# Build
./gradlew build

# Test
./gradlew test
\`\`\`

## Architecture
- **Tech Stack**: [Основные технологии]
- **Structure**: [Краткое описание структуры]

## Common Tasks
### Добавление новой функции
1. [Шаг 1]
2. [Шаг 2]
```

#### 2. Используйте Planning Mode для сложных задач

```bash
# Всегда начинайте большие задачи с планирования
/plan
Мне нужно рефакторить модуль database для улучшения производительности
```

#### 3. Создайте библиотеку slash команд

```
.claude/commands/
├── review.md          # Код ревью
├── test.md           # Генерация тестов
├── refactor.md       # Рефакторинг
├── docs.md           # Документация
├── fix.md            # Исправление багов
└── optimize.md       # Оптимизация
```

#### 4. Используйте Subagents для специализации

```
.claude/agents/
├── tester/          # Специалист по тестированию
├── reviewer/        # Эксперт по качеству кода
├── documenter/      # Технический писатель
└── optimizer/       # Эксперт по производительности
```

#### 5. Интегрируйте MCP Servers

```json
{
  "mcpServers": {
    "github": "Для работы с GitHub",
    "postgres": "Для доступа к базе данных",
    "playwright": "Для тестирования UI"
  }
}
```

### ❌ Распространенные ошибки

#### Ошибка 1: Недостаточно контекста

❌ **Плохо:**
```bash
"Исправь баг в коде"
```

✅ **Хорошо:**
```bash
"""
Прочитай файл database/src/commonMain/kotlin/ru/pavlig43/database/data/declaration/dao/DeclarationDao.kt

Проблема: При вызове getDeclarationById(null) выбрасывается NPE.

Задача:
1. Найди проблему в коде
2. Добавь проверку на null
3. Добавь unit тест для этого случая
"""
```

#### Ошибка 2: Слишком сложные запросы

❌ **Плохо:**
```bash
"Перепиши весь проект используя новую архитектуру, добавь тесты, документацию, оптимизируй производительность и задеплой в продакшн"
```

✅ **Хорошо:**
```bash
# Начните с планирования
/plan
Хочу переписать проект используя Clean Architecture

Этап 1: Создай план миграции
Этап 2: Начни с одного модуля
Этап 3: Проверь результат
```

#### Ошибка 3: Игнорирование CLAUDE.md

❌ **Плохо:**
Не создавать CLAUDE.md или оставлять его пустым

✅ **Хорошо:**
Поддерживать актуальный CLAUDE.md с:
- Текущими командами сборки
- Архитектурой проекта
- Стандартами кодирования
- Распространенными задачами

#### Ошибка 4: Не проверять изменения

❌ **Плохо:**
Применять все изменения без проверки

✅ **Хорошо:**
```bash
# После изменений Claude Code
./gradlew test
./gradlew detektAll
git diff  # Проверить изменения
```

### 🚀 Советы по продуктивности

#### 1. Используйте алиасы для команд

```bash
# ~/.bashrc или ~/.zshrc
alias claude-review='/claude review'
alias claude-test='/claude test'
alias claude-plan='/claude plan'
```

#### 2. Создайте шаблоны для проектов

```
~/.claude-templates/
├── kotlin-kmp/      # Шаблон для Kotlin Multiplatform
├── react-app/       # Шаблон для React приложения
└── python-service/  # Шаблон для Python сервиса
```

#### 3. Используйте Git hooks

```bash
# .git/hooks/pre-commit
#!/bin/bash
# Запускать Detekt перед коммитом
./gradlew detektAll
```

#### 4. Интегрируйте с IDE

- Создайте Custom Actions в Android Studio/IntelliJ
- Используйте External Tools для запуска slash команд
- Настройте hotkeys для частых операций

#### 5. Документируйте процессы

```markdown
# workflows/feature-development.md

## Process: Adding a New Feature

1. Planning
   ```bash
   /plan-feature --NAME="user-dashboard"
   ```

2. Implementation
   - Create module structure
   - Implement domain layer
   - Implement data layer
   - Implement presentation layer

3. Testing
   ```bash
   /test-agent features/userdashboard
   ```

4. Code Review
   ```bash
   /reviewer-agent features/userdashboard
   ```

5. Documentation
   ```bash
   /document-agent features/userdashboard
   ```

6. Create PR
   ```bash
   /pr-create --TITLE="Add user dashboard"
   ```
```

### 📋 Пример полного рабочего процесса

#### Сценарий: Добавление новой функции в проект Nocombro

```bash
# 1. Планирование
/plan
Хочу добавить функцию массового импорта деклараций из CSV файла.

Требования:
- Загрузка CSV файла
- Валидация данных
- Сохранение в базу данных
- Показ прогресса
- Обработка ошибок

# 2. Создание плана
Claude Code создаст подробный план с шагами

# 3. Реализация (по шагам из плана)
"Начни с шага 1: Создай структуру для CSV импорта"

# 4. После каждого шага - тесты
/test-agent CsvImportComponent

# 5. Ревью кода
/reviewer-agent features/csv-import

# 6. Документация
/document-agent features/csv-import

# 7. Создание PR
/pr-create --TITLE="Add CSV import for declarations"

# 8. После мерджа - обновить CLAUDE.md
"Обнови CLAUDE.md, добавив информацию о новой функции CSV импорта"
```

### 📊 Итоговые выводы

#### Что вы learned:
1. ✅ Установка и настройка Claude Code
2. ✅ Создание и поддержка CLAUDE.md
3. ✅ Работа с контекстом
4. ✅ Настройка инструментов и разрешений
5. ✅ Использование Planning и Thinking modes
6. ✅ Создание Slash Commands
7. ✅ Интеграция MCP Servers
8. ✅ Создание Subagents
9. ✅ Работа с GitHub
10. ✅ Лучшие практики

#### Следующие шаги:
- 📚 Изучить официальную документацию: [docs.anthropic.com](https://code.claude.com/docs/en/quickstart)
- 🔗 Присоединиться к сообществу: [GitHub Discussions](https://github.com/anthropics/claude-code/discussions)
- 💡 Экспериментировать с собственными workflow
- 🚀 Создавать свои MCP Servers
- 📝 Делиться своими slash commands и subagents

---

## 🔗 Полезные ресурсы

### Основные источники:
- [Claude Code Tutorial Playlist - Net Ninja](https://www.youtube.com/playlist?list=PL4cUxeGkcC9g4YJeBqChhFJwKQ9TRiivY)
- [Claude Code Official Documentation](https://code.claude.com/docs/en/quickstart)
- [MCP Official Documentation](https://modelcontextprotocol.io/docs/develop/build-server)

### Дополнительные ресурсы:
- [Writing a good CLAUDE.md](https://www.humanlayer.dev/blog/writing-a-good-claude-md)
- [CLAUDE.md Templates - GitHub](https://github.com/ruvnet/claude-flow/wiki/CLAUDE-MD-Templates)
- [Claude Code: Best practices](https://www.anthropic.com/engineering/claude-code-best-practices)
- [Extend Claude with skills](https://code.claude.com/docs/en/skills)
- [Connect Claude Code to tools via MCP](https://code.claude.com/docs/en/mcp)
- [Claude Code Academy](https://academy.claude-code.club/courses/claude-code-tutorial)

---

## 📌 Применение в вашем проекте Nocombro

Судя по вашему `CLAUDE.md`, вы уже следуете многим лучшим практикам из курса:
- ✅ Детальное описание архитектуры проекта
- ✅ Описание модулей и их назначения
- ✅ Инструкции по сборке и тестированию
- ✅ Информация о ключевых технологиях

### 🎯 Что можно улучшить на основе курса:

1. **Добавить секцию про Git команды**
   ```bash
   ## Git Workflow
   ### Feature development
   ```bash
   # Create feature branch
   git checkout -b feature/feature-name

   # Commit changes
   git add .
   git commit -m "feat: add feature description"

   # Push and create PR
   git push origin feature/feature-name
   /pr-create --TITLE="Add feature"
   ```
   ```

2. **Создать кастомные slash-команды для частых операций**
   ```
   .claude/commands/
   ├── build-deps.md      # Работа с зависимостями
   ├── kmp-module.md      # Создание KMP модулей
   ├── detekt-fix.md      # Исправление detekt ошибок
   └── release.md         # Подготовка релиза
   ```

3. **Добавить информацию о MCP-серверах если используете**
   ```markdown
   ## MCP Servers
   ### Vision MCP
   Для анализа UI дизайнов и скриншотов используется Vision MCP Server.

   ### Web Search MCP
   Для поиска актуальной документации используется Web Search MCP Server.
   ```

4. **Создать тестовые скрипты в секции Testing**
   ```bash
   ## Testing
   ### Quick test
   ```bash
   # Быстрый тест только для измененных модулей
   ./gradlew test --continuous
   ```

   ### Full test suite
   ```bash
   # Все тесты с покрытием
   ./gradlew test detektAll
   ```
   ```

5. **Добавить секцию Troubleshooting**
   ```markdown
   ## Troubleshooting

   ### Общие проблемы
   #### OutOfMemoryError при сборке
   ```bash
   ./gradlew build -Dorg.gradle.jvmargs="-Xmx4096m"
   ```

   #### Detekt находит ошибки в новом коде
   ```bash
   # Автоисправление где возможно
   ./gradlew detektAutoCorrect

   # Или попросите Claude исправить
   /detekt-fix database/src/commonMain/...
   ```
   ```

---

**Дата создания**: 31 января 2026
**Обновлено**: 31 января 2026
**Источник**: [YouTube Playlist - Claude Code Tutorial by Net Ninja](https://www.youtube.com/playlist?list=PL4cUxeGkcC9g4YJeBqChhFJwKQ9TRiivY)

**Примечание**: Это руководство создано на основе плейлиста Net Ninja и дополнительных ресурсов. Все примеры кода и команд готовы к использованию и могут быть адаптированы под ваш проект.
