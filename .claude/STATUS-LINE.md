# Status Line - Информационная строка в терминале

## Что добавлено

**`.claude/settings.local.json`:**
```json
{
  "statusLine": {
    "type": "command",
    "command": "bash .claude/scripts/status-info.sh"
  }
}
```

## Что показывает Status Line

```
📁 Nocombro | 🌿 feature/claude-skills-system | 📝 5 files
```

- **📁 Директория** — текущая папка проекта
- **🌿 Ветка** — текущая git ветка
- **📝 Файлы** — количество модифицированных файлов

---

## Контекст и токены

### Встроенная команда `/context`

Для просмотра информации о контексте и токенах используй:

```
> /context
```

**Покажет:**
- Использованные токены
- Доступные токены
- Процент использования
- Список загруженных файлов

### Скрипт context-stats.sh

Дополнительный скрипт для детальной статистики:

```bash
bash .claude/scripts/context-stats.sh
```

**Вывод:**
```
📊 45230/200000 tokens (22%) 🟢
```

- **🟢** — < 50% использовано
- **🟡** — 50-80% использовано
- **🔴** — > 80% использовано

---

## Настройка

### Изменить формат status line

Редактируй `.claude/scripts/status-info.sh`:

```bash
#!/bin/bash

# Текущая ветка
BRANCH=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo "no-git")

# Модифицированные файлы
MODIFIED=$(git status --short 2>/dev/null | wc -l | tr -d ' ')

# Текущая директория
DIR=$(basename "$PWD")

# Свой формат
echo "[$DIR] $BRANCH | M:$MODIFIED"
```

### Добавить больше информации

**Пример с временем:**
```bash
TIME=$(date +%H:%M)
echo "🕐 $TIME | 📁 $DIR | 🌿 $BRANCH"
```

**Пример с контекстом:**
```bash
CONTEXT=$(find . -name "*.kt" | wc -l | tr -d ' ')
echo "📁 $DIR | Kotlin: $CONTEXT файлов"
```

---

## Устранение проблем

### Status line не отображается

1. Проверь что скрипт исполняемый:
```bash
chmod +x .claude/scripts/status-info.sh
```

2. Проверь путь в `settings.local.json`:
```json
{
  "statusLine": {
    "type": "command",
    "command": "bash .claude/scripts/status-info.sh"
  }
}
```

3. Перезапусти Claude Code

### На Windows путь к bash

Если не работает, укажи полный путь:
```json
{
  "statusLine": {
    "type": "command",
    "command": "C:\Users\user\AppData\Local\Programs\Git\bin\bash.exe .claude/scripts/status-info.sh"
  }
}
```

---

## Дополнительные команды

### Показать размер контекста

```
> Какой размер текущего контекста?
> Сколько токенов использовано?
```

### Управление контекстом

```
> Очисти контекст
> /compact
```

---

**Источник:** [Claude Code Settings](https://code.claude.com/docs/en/settings)
