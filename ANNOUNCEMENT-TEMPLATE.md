# 📢 Сообщение для команды (готовый шаблон)

## Вариант для Slack/Telegram/Discord

```
🔥 ВАЖНО: Pre-commit Detekt Hooks

Команда, мы добавили автоматическую проверку кода перед коммитом!

🎯 Что это значит:
- Автоматическая проверка code style
- Автоформатирование кода
- Меньше ошибок в PR

🚀 Что нужно сделать (1 минута):

1. Обновить master:
   git checkout master && git pull

2. Установить hooks:
   ./gradlew installGitHooks

✅ Всё! Теперь при коммите Detekt будет проверять код автоматически.

Если Detekt найдет ошибки - коммит отклонится. Исправьте и повторите.

📚 Полная инструкция: CONTRIBUTING.md

❓ Вопросы? Пишите сюда!
```

---

## Вариант для Email

```
Тема: 📢 Важное обновление: Pre-commit Detekt Hooks

Привет, команда!

Мы внедрили автоматическую проверку кода через Detekt перед каждым коммитом.

Почему это важно:
✓ Единый code style для всего проекта
✓ Раннее обнаружение ошибок
✓ Автоформатирование кода
✓ Меньше правок при code review

Что нужно сделать (займет 2 минуты):

1. Обновите ветку master:
   $ git checkout master
   $ git pull origin master

2. Установите Git hooks:
   $ ./gradlew installGitHooks

   Должно появиться: "✅ Git pre-commit hook installed"

3. Проверьте установку:
   $ ls .git/hooks/pre-commit  # Linux/macOS
   > dir .git\hooks\pre-commit  # Windows

Как это работает теперь:

git commit -m "feat: new feature"
↓
Автоматически запускается Detekt
↓
✅ Если ошибок нет - коммит выполняется
❌ Если есть ошибки - показывает список и отклоняет

Важно знать:

1. Detekt может менять ваш код (автоформатирование)
   Если коммит прервался с "Detekt made changes":
   - Посмотрите: git diff
   - Добавьте: git add .
   - Повторите: git commit ...

2. Можно пропустить проверку (только в крайнем случае!):
   git commit --no-verify -m "WIP"

3. Запустить Detekt вручную:
   ./gradlew detektAll

Полная документация:
📗 CONTRIBUTING.md - руководство для контрибьюторов
📗 .github/TEAM-ANNOUNCEMENT.md - детальное описание

GitHub Actions:
Теперь Detekt также автоматически проверяет все PR и комментирует результаты.

Вопросы?
Создайте Issue или напишите в чат.

Спасибо!
Команда разработки
```

---

## Краткая шпаргалка (Quick Reference)

```
📋 Установка
./gradlew installGitHooks

🔍 Проверка
./gradlew detektAll

🚫 Пропуск (только если надо!)
git commit --no-verify -m "message"

❓ Проблемы?
См. CONTRIBUTING.md
```

---

## Для Reminder (можно пингануть через неделю)

```
🔔 Напоминание: Detekt Hooks

Привет! Установили pre-commit hooks?

./gradlew installGitHooks

Если нет - см CONTRIBUTING.md за 2 минутами инструкций!
```
