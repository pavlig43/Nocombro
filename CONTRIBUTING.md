# Contributing to Nocombro

Спасибо за интерес к проекту Nocombro! Этот документ поможет вам настроить среду разработки и внести свой вклад.

## 📋 Содержание

- [Установка](#установка)
- [Настройка Git Hooks](#настройка-git-hooks)
- [Code Style](#code-style)
- [Процесс работы](#процесс-работы)
- [Решение проблем](#решение-проблем)

---

## Установка

### Требования

- JDK 17+
- Kotlin 1.9+
- Git

### Клонирование репозитория

```bash
git clone https://github.com/pavlig43/Nocombro.git
cd Nocombro
```

### Установка зависимостей

```bash
./gradlew build
```

---

## Настройка Git Hooks

### 🔥 Обязательно: Установите pre-commit hooks

Проект использует **Detekt** для статического анализа кода. Pre-commit hooks автоматически проверяют ваш код перед коммитом.

#### Установка (выполните один раз)

```bash
./gradlew installGitHooks
```

#### Как работают hooks

При выполнении `git commit` автоматически запускается:

1. **Detekt formatting** - автоформатирование кода
2. **Detekt check** - проверка на соответствие code style

Если Detekt найдет проблемы:
- Коммит будет **отклонен**
- Detekt покажет список проблем
- После исправления нужно повторить коммит

#### Пропуск hooks (только в исключительных случаях)

```bash
git commit --no-verify -m "message"
```

> ⚠️ Используйте `--no-verify` только когда действительно необходимо!

---

## Code Style

### Detekt

Мы используем [Detekt](https://detekt.dev/) для статического анализа Kotlin кода.

#### Запуск Detekt вручную

```bash
# Проверить все модули
./gradlew detektAll

# Проверить конкретный модуль
./gradlew :core:detektAndroidDebug
./gradlew :core:detektDesktopMain

# Форматирование кода (autoCorrect включен по умолчанию)
./gradlew detektAll
```

#### Конфигурация

Detekt конфиг находится в: `default-detekt-config.yml`

Основные настройки:
- **MaxLineLength**: 180 символов (Compose генерирует длинные строки)
- **AutoCorrect**: включен
- **Strict mode**: включен (ошибки блокируют коммит)

#### Частые проблемы Detekt

| Правило | Описание | Решение |
|---------|----------|---------|
| `ExplicitItLambdaParameter` | Нельзя использовать `it` как имя параметра в сложных лямбдах | Используйте явное имя параметра |
| `MaxLineLength` | Строка длиннее 180 символов | Разбейте строку или переносите выражения |
| `MagicNumber` | В коде есть "магические числа" | Вынесите в константу |
| `UnusedPrivateMember` | Неиспользуемые приватные элементы | Удалите или добавьте `@Suppress` |

---

## Процесс работы

### 1. Создайте ветку

```bash
git checkout -b feature/your-feature-name
# или
git checkout -b fix/bug-description
```

### 2. Внесите изменения

```bash
# Работайте с кодом
# ...
```

### 3. Перед коммитом

```bash
# Проверьте изменения
git status
git diff

# Добавьте файлы
git add path/to/file

# Коммит (автоматически запустится Detekt)
git commit -m "feat: add new feature"
```

Если Detekt найдет проблемы:
```bash
# Detekt покажет ошибки
# Исправьте их

# Повторите коммит
git add .
git commit -m "feat: add new feature"
```

### 4. Отправьте изменения

```bash
git push origin feature/your-feature-name
```

### 5. Создайте Pull Request

На GitHub создайте PR из вашей ветки.

**GitHub Actions автоматически:**
- Запустит Detekt
- Прокомментирует результаты в PR
- Создаст отчеты (HTML/MD) как артефакты

---

## Решение проблем

### Hook не запускается

```bash
# Переустановите hooks
./gradlew installGitHooks

# Проверьте наличие файла
ls .git/hooks/pre-commit  # Linux/macOS
dir .git\hooks\pre-commit # Windows
```

### Detekt находит ошибки, но это false positives

**Вариант 1:** Отключить правило для конкретного кода
```kotlin
@Suppress("RuleName")
fun myFunction() { ... }
```

**Вариант 2:** Обновить `default-detekt-config.yml` (согласуйте с командой!)

### Hook тормозит коммит

Первый запуск долгий (кэширование), последующие - быстрые.

Для ускорения:
```bash
./gradlew detektAll --build-cache
```

### Windows: проблемы с PowerShell

Убедитесь, что:
1. PowerShell Execution Policy позволяет запуск скриптов
2. Файл `.git/hooks/pre-commit` существует и вызывает PowerShell скрипт

```powershell
# Проверьте Execution Policy
Get-ExecutionPolicy

# Если Restricted, установите RemoteSigned
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

---

## Полезные команды

```bash
# Установка hooks
./gradlew installGitHooks

# Запустить Detekt
./gradlew detektAll

# Очистка проекта
./gradlew clean

# Сборка проекта
./gradlew build

# Запуск Desktop приложения
./gradlew :app:desktopApp:run

# Запуск Android приложения
./gradlew :app:nocombroapp:installDebug
```

---

## Ресурсы

- [Detekt Documentation](https://detekt.dev/)
- [Kotlin Coding Conventions](https://kotlinlang.org/docs/coding-conventions.html)
- [Compose Guidelines](https://developer.android.com/jetpack/compose)
- [Decompose Documentation](https://arkivanov.github.io/Decompose/)

---

## Контакты

По вопросам обращайтесь через:
- GitHub Issues
- GitHub Discussions

---

**Happy Coding! 🚀**
