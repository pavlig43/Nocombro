# Запросы для PNG-концептов

Источник: `current/current-04-products-list.png`.

## A

```text
Use case: ui-mockup. High-fidelity Kotlin Compose desktop UI.
Redesign only the Products list as a compact workbench table matching the new calm card-based forms.
Keep the Nocombro top bar, breadcrumbs, dark theme and mustard accent. Add a rounded one-line command card and a large rounded table card with a neutral header, restrained dividers, selection state and compact footer.
Palette: #0D0F0F, #171A19, #303531, #D7A91E, off-white text.
Avoid gradients, glass, charts, sidebar, huge title, watermark and extra logos.
```

## B

```text
Use case: ui-mockup. High-fidelity Kotlin Compose desktop UI.
Redesign only the Products list as a master-detail workspace matching the new card-based forms.
Keep the Nocombro shell. Use about 62% width for a dense table and 38% for a quick preview card with one action to open the full form.
Use the same dark palette and mustard accent. Avoid gradients, glass, charts, sidebar, huge title, watermark and extra logos.
```

