# Git Hooks & Detekt Setup

## 📋 Что это?

Автоматические pre-commit hooks, которые:
1. **Форматируют код** через Detekt (`--autoCorrect`)
2. **Проверяют код** через Detekt

## 🚀 Установка

### Вариант 1: Автоматическая (рекомендуется)
```bash
./gradlew installGitHooks
```
или просто запустите любую сборку:
```bash
./gradlew build
```

### Вариант 2: Ручная
```bash
# Linux/macOS
cp scripts/pre-commit .git/hooks/
chmod +x .git/hooks/pre-commit

# Windows
cp scripts/pre-commit.ps1 .git/hooks/
```

## 🔄 Как это работает

### При коммите:
1. Вы делаете `git commit`
2. Hook запускает `./gradlew detektAll --autoCorrect`
3. Hook запускает `./gradlew detektAll`
4. Если Detekt внес изменения → коммит прерывается
5. Вы смотрите изменения, добавляете их и коммитите снова

### Пропуск hook (если нужно):
```bash
git commit --no-verify -m "WIP message"
```

## 🧪 Тестирование

### Запустить Detekt вручную:
```bash
# Форматирование
./gradlew detektAll --autoCorrect

# Проверка
./gradlew detektAll
```

### GitHub Actions
Detekt автоматически запускается на:
- Push в `master`, `main`, `develop`
- Pull Request в эти ветки

Результаты:
- ✅ Комментарий в PR с отчетом
- 📊 Артефакты с HTML/MD отчетами

## 📁 Структура

```
scripts/
├── pre-commit         # Bash hook для Linux/macOS
├── pre-commit.ps1     # PowerShell hook для Windows
└── README.md          # Этот файл
```

## ⚙️ Настройка

### Отключить автоматическую установку hooks
Отредактируйте `build-logic/convention/src/main/java/DetektPlugin.kt`:
```kotlin
// Закомментируйте эту строку:
// dependsOn("installGitHooks")
```

### Изменить поведение Detekt
Отредактируйте `default-detekt-config.yml` в корне проекта.

## 🐛 Troubleshooting

### Hook не запускается
```bash
# Проверьте права доступа
ls -la .git/hooks/pre-commit

# Переустановите hook
./gradlew installGitHooks
```

### Detekt находит ошибки, но это false positives
1. Отредактируйте `default-detekt-config.yml`
2. Или добавьте `@Suppress` в код

### Hook тормозит коммит
Detekt кэшируется, первый запуск долгий, последующие быстрые.
Для ускорения:
```bash
./gradlew detektAll --build-cache
```

## 📚 Полезные команды

```bash
# Установить hooks
./gradlew installGitHooks

# Запустить Detekt на всех модулях
./gradlew detektAll

# Запустить Detekt на конкретном модуле
./gradlew :core:detektAndroidDebug

# Сгенерировать HTML отчет
./gradlew detektAll
# Отчеты здесь: */build/reports/detekt/
```
